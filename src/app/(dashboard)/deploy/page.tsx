export default function DeployPage(): React.ReactElement {
  return (
    <div>
      <h1 className="text-section text-on-surface">배포 관리</h1>
      <p className="mt-2 text-on-surface-variant">JSON-LD 코드를 쇼핑몰에 적용합니다.</p>
    </div>
  );
}
