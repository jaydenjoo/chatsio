export default function LoginPage(): React.ReactElement {
  return (
    <div className="flex min-h-screen items-center justify-center bg-surface">
      <h1 className="text-section text-on-surface">로그인</h1>
    </div>
  );
}
